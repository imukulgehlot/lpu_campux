// Row(children: [
// if (widget.postModel.likeCountsTypeWise != null)
// if (widget.postModel.likeCountsTypeWise!
//     .containsKey(listLikeType?.first.id))
// Container(
// padding: EdgeInsets.symmetric(
// horizontal: Dimensions.w6,
// vertical: Dimensions.h3),
// decoration: BoxDecoration(
// borderRadius:
// BorderRadius.circular(Dimensions.r10),
// color: Get.theme.colorScheme.hintTextColor),
// child: Row(children: [
// CustomSvgAssetImage(
// image: listLikeType!.first.svgCode!,
// isSVGString: true,
// width: Dimensions.w25,
// height: Dimensions.w25,
// )
// ]),
// )
// ])
